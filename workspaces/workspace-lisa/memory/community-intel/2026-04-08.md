# OpenClaw Community Intelligence Report — 2026-04-08

**Generated:** 2026-04-10 10:00 SGT (UTC+8)

---

## 1. New Features & Updates

### Latest Release: openclaw 2026.4.9 (Apr 9, 2026)

**Memory/Dreaming System (Major New Feature):**
- **Grounded REM backfill lane** with `rem-harness --path`, diary commit/reset flows, durable-fact extraction, and short-term promotion integration
- Old daily notes can now replay into Dreams and durable memory without a second memory stack
- **Control UI diary view** with timeline navigation, backfill/reset controls, traceable dreaming summaries, and a grounded Scene lane with promotion hints

**Other Features:**
- **QA Lab: character-vibes evaluation reports** with model selection and parallel runs for comparing candidate behavior
- **Plugin provider auth aliases** — providers can now share env vars, auth profiles, and config-backed auth across variants
- **iOS release versioning** — CalVer pinning in `apps/ios/version.json`, TestFlight iteration tied to gateway version, `pnpm ios:version:pin -- --from-gateway` workflow
- **Ollama thinking output** — models using native `api: "ollama"` path can now display thinking output when `/think` is set to non-off
- **Codex CLI system prompt passthrough** via `model_instructions_file` config override
- **Plugin SDK exports** — channel plugin base and web-search config contract now in public package

---

## 2. Bug Reports & Issues

### Fixed in 2026.4.9 (Community-Relevant):

| Area | Issue | Impact |
|------|-------|--------|
| **Browser/Security** | SSRF bypass via interaction-driven navigations — blocked-destination checks now re-run after clicks/evals (#63226) | **Critical security fix** — browser automation could previously bypass URL restrictions |
| **Security/dotenv** | Runtime-control env vars blocked from untrusted workspace `.env` files (#62660, #62663) | Security hardening |
| **Node exec events** | Remote node output sanitized — can no longer inject trusted `System:` content (#62659) | **Critical security fix** |
| **Android pairing** | Stale setup-code auth cleared on new QR scans, backgrounded app pauses auto-retry (#63199) | **Important for mobile users** — fixes unreliable Android pairing |
| **Slack media** | Bearer auth preserved across same-origin `files.slack.com` redirects (#62960) | Slack image attachments now load properly |
| **Sessions routing** | `sessions_send` follow-ups no longer steal delivery from Telegram/Discord (#58013) | **Important** — fixes cross-channel delivery issues |
| **Gateway sessions** | `/reset` and `/new` clear auto-fallback model overrides but preserve explicit user selections (#63155) | Model switching now works correctly |
| **Matrix stability** | Sync readiness check before startup, background handler failures contained (#62779) | Matrix channel no longer crashes gateway |
| **OpenAI reasoning** | Default missing reasoning effort to `high` on Responses/WebSocket transports | Better default behavior |
| **Windows update** | Added heap headroom to `pnpm build` steps — update preflight builds no longer fail on low memory | |

### Open Issues (Community-Monitored):

- **Browser automation timeouts** — persistent timeout on `act` with `profile="openclaw"` (#11518, closed but related issues remain)
- **Fill/click timeouts on textbox elements** (#43012, open)
- **File upload flow flaky** — misreports stale-click as browser control timeouts (#38844)
- **Browser sessions consistently timing out** (#43654, open)
- **ACP sessions accumulate orphan processes** — memory leak (#61895)
- **CLI hangs after 2026.4.5 upgrade** (#62335, open)

---

## 3. Tips & Workarounds

### From Community & Release Notes:

1. **Ollama thinking display**: Set `/think` to a non-off level to see Ollama model reasoning — now works with native `api: "ollama"` path
2. **Codex CLI system prompt**: OpenClaw system prompt now passes through to Codex CLI via `model_instructions_file` — better agent coherence
3. **Android pairing fix**: If pairing is flaky, re-scan QR — stale auth is now properly cleared on new scans
4. **Model overrides**: `/reset` and `/new` now properly clear auto-fallback model overrides while keeping your explicit choices
5. **Browser security**: After v2026.4.9, browser interactions that navigate to blocked URLs are properly caught — update if using browser automation
6. **`.env` security**: Untrusted workspace `.env` files can no longer inject runtime-control or browser-control env vars
7. **Matrix DM policy**: `openclaw doctor --fix` will migrate legacy `"trusted"` DM policy to compatible policies
8. **Windows builds**: If `pnpm build` fails on Windows with OOM, update to 2026.4.9+ which adds Node heap headroom

### From DEV.to Article ("What OpenClaw Gets Wrong Out of the Box"):

- Default configs often overlooked — review and customize for your use case
- Agent timeout and model settings benefit from explicit configuration

---

## 4. Notable Discussions & Community Activity

- **CVE-2026-32918**: Sandbox breakout allowing access to isolated session state — patched in recent versions. Ensure you're on 2026.4.9+
- **Community repo updated** (Apr 8): Moderation docs, onboarding, and incident playbook updated
- **Plugin SDK expansion**: Public package now exports channel plugin base and web-search contract — easier plugin development
- **iOS TestFlight**: New CalVer versioning workflow for more predictable release trains
- **ClawHub skill search** now in Control UI (#60134, merged) — browse and install skills from the UI

---

## 5. Browser Automation Status (Our System)

- **CDP Port 18800**: ✅ Active and responsive
- **Chrome Version**: HeadlessChrome/145.0.7632.0 (CDP reports Chrome/145.0.7632.0)
- **CDP WebSocket**: Connected at `ws://localhost:18800/devtools/browser/...`
- **System Chrome**: v146.0.7680.177 running (Wilson's desktop Chrome)
- **Status**: Browser automation operational

### Security Note:
v2026.4.9 fixes a browser SSRF bypass vulnerability (#63226). If we update OpenClaw, this fix will prevent browser automation from navigating to blocked URLs after interaction-driven navigations. Current version may be affected — **recommend updating to 2026.4.9**.

---

## 6. Action Items

1. ⬆️ **Update OpenClaw to 2026.4.9** — Critical security fixes (browser SSRF, node exec injection, dotenv leak)
2. 📱 **Test Android pairing** — The new fix should improve reliability
3. 🔧 **Review `.env` files** — New security policy blocks untrusted workspace env vars
4. 📊 **Try Memory/Dreaming features** — Grounded REM backfill and diary view could benefit our memory system
5. 🛠️ **Monitor browser timeout issues** — Still open bugs in fill/click and upload flows